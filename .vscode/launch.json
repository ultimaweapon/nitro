{
  "configurations": [
    {
      "name": "Nitro",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/dist/bin/nitro",
      "args": [
        "build",
        "${workspaceFolder}/std"
      ],
      "cwd": "${workspaceFolder}",
      "env": {
        "RUST_BACKTRACE": "1"
      },
      "preLaunchTask": "build"
    }
  ],
  "version": "2.0.0"
}
